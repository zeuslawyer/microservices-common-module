{
  "name": "@zeuscoder-public/microservices-course-shared",
  "version": "1.0.2",
  "description": "common module with shared code for kube",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"update...\" && npm version patch && npm run build and npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^3.9.5"
  }
}
